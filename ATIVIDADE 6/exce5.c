#include <stdio.h>

int main() {
    float numeros [6;]
    int i;
    float soma = 0, media;
    
    // Leia os valores
    // Calcule a media
    
    printf ("Media = %.2f\n" media);
    return 0;
}