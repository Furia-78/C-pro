#include <stdio.h>

int main() {
    float vendas[7];
    int i, diaMaior = 0;
    float maior;
    
    // Leia as vendas
    // Descubra o maior valor e o dia correspondente
    
    printf ("maior venda = %.2f no dia %d\n", maior, diaMaior + 1);
    
    return 0;
}